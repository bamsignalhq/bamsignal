/** Customer Support Center™ indexable paths */
export const SUPPORT_CENTER_PATHS = ["/help", "/contact", "/tickets", "/knowledge-base"];

export function getSupportCenterIndexablePaths() {
  return [...SUPPORT_CENTER_PATHS];
}
