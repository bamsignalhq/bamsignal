/** Keep in sync with android/app/build.gradle defaultConfig. */
export const APP_VERSION_NAME = "1.0.11";
export const APP_VERSION_CODE = 14;
export const APP_BUILD_LABEL = `${APP_VERSION_NAME} (${APP_VERSION_CODE})`;
