/** Public Signal Concierge routes indexed for SEO — member-only paths excluded. */
export function getSignalConciergeIndexablePaths() {
  return ["/signal-concierge", "/signal-concierge/privacy", "/signal-concierge/faq"];
}
